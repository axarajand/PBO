1. Jelaskan apa yang dimaksud dengan class dan object!
    Class: Class adalah sebuah blueprint atau cetakan untuk menciptakan objek. Class mendefinisikan atribut (variabel) dan metode (fungsi) yang akan dimiliki oleh objek yang dibuat berdasarkan class tersebut. Class dalam pemrograman berorientasi objek (OOP) berfungsi sebagai template untuk menciptakan objek dengan karakteristik tertentu.
    Objek: Objek adalah instance (penyajian konkret) dari class. Dalam arti sederhana, objek adalah variabel yang dihasilkan dari class. Ketika class diinstansiasi, objek baru dibuat berdasarkan template yang didefinisikan oleh class tersebut. Objek memiliki atribut dan metode yang diwarisi dari class.

2. Sebutkan dan jelaskan jenis-jenis method !
    Method Instance: Method instance adalah metode yang bekerja dengan objek instance dari sebuah class. Method ini biasanya digunakan untuk memanipulasi atau mengakses data yang terkait dengan objek tertentu.
    Method Kelas (Class Method): Method kelas adalah metode yang terkait dengan class itu sendiri daripada dengan instance individu dari class. Mereka dapat digunakan untuk mengakses atau memanipulasi atribut kelas dan untuk membuat instance kelas baru.
    Method Statis (Static Method): Method statis adalah metode yang tidak terkait dengan objek atau class tertentu. Mereka tidak memiliki akses ke atribut kelas atau instance dan sering digunakan untuk fungsi-fungsi yang terkait dengan class namun tidak memerlukan objek.
    Method Dunder (Magic Method): Method-method dunder adalah metode bawaan dalam Python yang diawali dan diakhiri dengan dua garis bawah (double underscore). Mereka memberikan fungsionalitas khusus seperti operator overloading, pembuatan objek, pengelolaan siklus hidup objek, dan lain-lain.
    Method Getter dan Setter: Method getter digunakan untuk mendapatkan nilai atribut objek, sementara method setter digunakan untuk mengubah nilai atribut objek. Ini membantu dalam menerapkan konsep enkapsulasi dan pengendalian akses ke atribut objek.
    Method Public, Protected, dan Private: Di dalam Python, ada konvensi untuk menandai aksesibilitas metode dengan menggunakan underscore (). Method yang diawali dengan satu underscore (), seperti _protected_method(), dianggap sebagai protected, sementara method yang diawali dengan dua underscore (__), seperti __private_method(), dianggap sebagai private. Method tanpa underscore atau dengan underscore ganda, seperti public_method(), dianggap sebagai public. Namun, ini lebih merupakan konvensi daripada penanganan langsung dari bahasa Python sendiri.

3. Berdasarkan gambar berikut ini, jelaskan masing-masing bagian sesuai
dengan nomor yang ada!

Deklarasi Class:
  Baris pertama (public class Komputer {) adalah deklarasi class Java dengan nama Komputer. Ini menandakan awal dari definisi class yang akan berisi properti (atribut) dan perilaku (method) dari objek yang akan dibuat berdasarkan class ini.

Deklarasi Atribut (Variabel):
  Baris kedua dan ketiga (String jenis_komputer; dan private String merk;) adalah deklarasi atribut atau variabel yang akan dimiliki oleh setiap objek yang dibuat dari class Komputer.
jenis_komputer dideklarasikan tanpa spesifier visibilitas (default visibility), sehingga dapat diakses oleh semua class dalam package yang sama.
merk dideklarasikan dengan spesifier private, sehingga hanya dapat diakses dari dalam class Komputer itu sendiri.

Deklarasi Method:
  Baris keempat hingga baris kesembilan (public void setDataKomputer(String jenis, String merk) {...}) adalah deklarasi method setDataKomputer() yang bertujuan untuk mengatur data jenis_komputer dan merk dari objek Komputer.
Method setDataKomputer() mengambil dua parameter yaitu jenis dan merk, kemudian mengatur nilai jenis_komputer dengan nilai dari parameter jenis, dan mengatur nilai merk dengan nilai dari parameter merk.

Method Getter:
  Baris kesepuluh hingga baris keempat belas (public String getJenis() {...}) adalah deklarasi method getJenis().
Method getJenis() digunakan untuk mengembalikan nilai atribut jenis_komputer.

Method Getter:
  Baris keenam belas hingga baris kedelapan belas (public String getMerk() {...}) adalah deklarasi method getMerk().
Method getMerk() digunakan untuk mengembalikan nilai atribut merk.

Main Method:
  Baris kedua puluh (public static void main(String[]args) {) adalah deklarasi method main. Method ini adalah entry point (titik masuk) dari program Java yang akan dijalankan.
Method main akan dijalankan terlebih dahulu ketika program Java dieksekusi.

Pembuatan Objek:
  Baris kedua puluh satu (Komputer mykom = new Komputer();) adalah pembuatan objek baru dari class Komputer dengan nama mykom.
Kata kunci new digunakan untuk mengalokasikan memori untuk objek baru, sedangkan Komputer() adalah panggilan ke constructor default yang diberikan secara implisit.

Pemanggilan Method dan Output:
  Baris kedua puluh dua dan kedua puluh tiga (mykom.setDataKomputer("Laptop", "MacBook"); dan System.out.println(mykom.getJenis()); serta System.out.println(mykom.getMerk());) adalah pemanggilan method yang telah didefinisikan sebelumnya.
Method setDataKomputer() dipanggil untuk mengatur data jenis_komputer dan merk pada objek mykom.
Method getJenis() dan getMerk() dipanggil untuk mengambil nilai jenis_komputer dan merk dari objek mykom, kemudian nilai-nilai tersebut dicetak menggunakan System.out.println().
